import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexBetweenColumn = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  flexDirection: "column",
}); 

export default FlexBetweenColumn;
 